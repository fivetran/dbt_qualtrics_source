name: 'qualtrics_source_integration_tests_02'
version: '0.1.1'
profile: 'integration_tests'
config-version: 2

vars:
  qualtrics_schema: qualtrics_source_integration_tests_02
  qualtrics__using_core_contacts: true
  qualtrics__using_core_mailing_lists: true

models:
  +persist_docs:
    relation: true
    columns: true

dispatch:
  - macro_namespace: dbt_utils
    search_order: ['spark_utils', 'dbt_utils']

seeds:
  qualtrics_source_integration_tests_02:
    +column_types:
      _fivetran_synced: timestamp
    directory_contact:
      +column_types:
        phone: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
